import WorkoutLibrary from '@/components/home/WorkoutLibrary';

const WorkoutPage = () => {
     return (
          <main>
               <WorkoutLibrary />
          </main>
     );
};

export default WorkoutPage;
